============
Installation
============

At the command line::

    $ easy_install django-logincidadao-provider

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-logincidadao-provider
    $ pip install django-logincidadao-provider
